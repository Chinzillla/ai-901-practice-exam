{
  "name": "ai-901-mock-exam-tool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:questions": "node scripts/build-question-dataset.mjs",
    "dev": "npm run generate:questions && vite --host 127.0.0.1",
    "build": "npm run generate:questions && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "vite": "^7.3.0"
  },
  "devDependencies": {}
}
